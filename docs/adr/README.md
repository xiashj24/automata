# Architecture Decision Records

One numbered markdown file per significant design decision, in Nygard format
(Status / Context / Decision / Consequences). Append-only: to reverse a
decision, add a new ADR that supersedes the old one instead of rewriting it.

0001–0005 were written together as the founding set for the standalone
rewrite; `docs/architecture.md` is the map that ties them together.
